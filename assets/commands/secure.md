---
description: Frame a non-sensitive security class on the exact pinned host.
skill: engineering-pursuit
---

Exact-host limitation: this pinned host has no transcript-excluded confidential-input channel. Do not put confidential or restricted objectives, findings, exploit details, secrets, summaries, evidence, or arbitrary metadata in command arguments. Accept only the closed non-sensitive kind, `trusted-workspace` scope, and `public-safe` classification framing. A `restricted` classification fails closed with `ENGINEERING_RESTRICTED_INPUT_UNSUPPORTED`. Foreground framing is ephemeral, non-resumable, and persistence-disabled. Infer no active-test, credential, network, disclosure, Git, publish, merge, deploy, or completion authority.
