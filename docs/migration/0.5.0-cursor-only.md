# Migrating to 0.5.0

Version 0.5.0 is a breaking Cursor-only boundary.

- Use `/curiosity-deliver-change` with three named read-only specialists and the bounded writable `curiosity-implementer` exposed by the Cursor manifest.
- Optimize for context quality and parent-context preservation: main orchestrates/synthesizes, delegates broad Explore, and never edits project source or runs project-mutating shell commands; only one assigned implementer writes.
- Treat main's no-edit boundary as a required semantic invariant, not host enforcement. Cursor children inherit the parent mode/tool envelope, so use Agent mode for writable hierarchy and do not expect Ask/Plan child elevation.
- Treat bounded curiosity as foundational across all substantive child work, including Explore—not as a new agent, skill, tool, or optional research step. Main applies the canonical rule's receipt gate, preserving the same child ID for bounded repair; this remains semantic policy without a runtime validator, hook, or state store.
- Remove references to former runtime entrypoints, installers, package exports, executable assets, and compatibility commands; no aliases or fallback runtime remain.
- Do not expect npm, global-install, service, hook, MCP, or state-store behavior.
- Treat material under `provenance/history/` as historical evidence only.

No automatic migration or cutover is performed.
