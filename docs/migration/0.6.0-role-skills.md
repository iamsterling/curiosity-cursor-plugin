# Migrating to 0.6.0

Version 0.6.0 preserves the Cursor-only static boundary, four role names, `/curiosity-deliver-change`, and `curiosity-delivery`. It replaces the one-skill inventory with five composable skills.

No compatibility aliases exist. Consumers should rediscover the plugin and confirm all five skill directories. Handoffs now name REQUIRED SKILLS semantically; this is not a programmatic attachment claim. Implementer handoffs must supply approved architecture decisions and allowed paths. Consequential boundary work now stops for strategist output and an explicit owner decision before edits.

Implementer results use `DONE|BLOCKED|OWNER_DECISION_REQUIRED` and typed reasons. Evidence uses the canonical capsule schema in implementation discipline. Reviewer results distinguish independently executed checks from audited summaries. Existing receipts and same-ID correction behavior remain.

No installer, hook, MCP, SDK, runtime, state migration, or cutover step is introduced. Run repository static verification; use the separate disposable live-smoke plan only under explicit approval.
