# Migrating to 0.7.0

Version 0.7.0 retains the Cursor-only boundary, `/curiosity-deliver-change`, all four role names, five skills, and the always-applied rule. It adds `/curiosity-bug`, `/curiosity-feature`, `/curiosity-deep-research`, `/curiosity-review`, `/curiosity-secure`, `/curiosity-verify`, and `/curiosity-architecture`.

Update local plugin loading to the 0.7.0 directory and reopen a separate Cursor session. Verify host discovery of exactly four agents, five skills, eight commands, and one rule. No new runtime, installer, hook, MCP, SDK, script, dependency, alias, or persistent state is introduced.

Commands express semantic Task routing. Cursor does not derive host-enforced scheduling or parent/child permissions from these prompts. If a required named agent, skill, or Task is unavailable, the command returns `BLOCKED_ROUTING`; main must not substitute specialist work.
