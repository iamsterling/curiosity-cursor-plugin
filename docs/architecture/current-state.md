# Current state

**Current (2026-08-15).** `iamsterling.curiosity-cursor-plugin` contains a research-phase OpenCode plugin foundation plus an additive native Cursor 0.4.0 surface. One OpenCode `Plugin.define` retains bundled-agent routing and generic redacted capture under `.opencode/curiosity-cursor-plugin/capture/v1/`.

The split still excludes the unfinished loop engine, Ledger lifecycle authority/runtime hooks/tools, graph proposal, typed engineering controllers, external records, local effects, and orchestration stub. `/loop-*` files remain markdown aliases; runtime requests fail closed with `CURIOSITY_CURSOR_COMPAT_RUNTIME_UNSUPPORTED`.

The native boundary is exactly six explicit markdown agents, one model-eligible `skills/curiosity-engineering/SKILL.md`, and the six-event stateless command-hook mesh. Four agents are read-only advisors. Worker and implementer use `readonly: false` only for exact accepted, ready Todos with exclusive ownership and marked v1 handoffs. Native Todos are attempted-work/progress projections, not evidence authority. After execution/delegation, the parent maps mandatory requirements, scenarios, and evidence commands to raw PASS/FAIL/MISSING in a separate prompt-level Verification Gate. FAIL/MISSING blocks finish confirmation regardless of Todo checkmarks, `All done`, or attempted confirmation. Material drift still requires revised native Plan acceptance.

The dispatcher handles only sessionStart, subagentStart, beforeShellExecution, beforeReadFile, Shell-matched postToolUse, and preCompact. It validates marked writable handoff shape, requests native approval for enumerated consequential command classes, denies exact supplied transcript-path access, and injects bounded guidance. It does not parse shell fully, inspect command output, declare evidence verdicts, read transcripts, persist state/logs, use network, follow up, schedule work, or control MCP. There are no MCP servers, rules, commands, variables, plugin-owned workflow state, custom lifecycle runtime, OpenSpec/Beads implementation, installation, or marketplace metadata.

The Cursor and OpenCode surfaces coexist; no cutover occurred. Actual Cursor sessions require authentication. CWD is the default workspace; selecting this repository may load root `AGENTS.md` as a project instruction. Keep target and plugin roots distinct with `agent --workspace <target> --plugin-dir <plugin-root>`. Workspace trust may prompt and saved trust, account, and session state may persist. Omitting `--plugin-dir` rolls back plugin loading only.

Explicit agents are `/curiosity-coordinator`, `/curiosity-worker`, `/curiosity-implementer`, `/curiosity-researcher`, `/curiosity-reviewer`, and `/curiosity-strategist`; automatic selection is nondeterministic. `readonly: true` documents no file edits and no state-changing shell commands. It is not a confidentiality/no-read/local-only/no-network guarantee or proof of prompt compliance. Historical live CLI evidence showed Cursor marking an evidence Todo completed and rendering `All done` after the mandatory suite exited 1; that remains the basis for progress-only Todos and the prompt-level Verification Gate. No live model smoke ran for v0.4.0, so the hook mesh has static/fixture evidence only. The editor remains unverified because Orca Accessibility failed systemically. No complete runtime-parity claim is made.
