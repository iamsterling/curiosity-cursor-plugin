# opencode2-config

Private bootstrap repository for `@iamsterling/opencode2-config`, a brand-new OpenCode 2 plugin identity built from the MIT-licensed OpenCode Loop implementation.

Status: repository shell only. Runtime implementation is imported in later provenance-separated commits. No installation cutover is performed by this bootstrap.

See [`docs/provenance.md`](docs/provenance.md) for source attribution and import verification.
