# Release manifest placeholder

Private Git distribution is the only approved bootstrap channel. A future release manifest may bind a commit, artifact hashes, exact OpenCode dependency, command inventory, and verification evidence. No Stage 3 installer contract, npm artifact, public release, or installation action is defined here.
