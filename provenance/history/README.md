# Historical provenance

Everything in this directory is historical evidence only. It is not current architecture, installed behavior, a compatibility promise, or a supported product surface.

- `decisions/` contains superseded ADRs 0001–0026.
- `docs/` contains superseded architecture, installation, migration, operations, provenance, research, release, and workflow material.
- `opencode-loop-CHANGELOG.md` preserves the imported upstream changelog.
- `relocations-v0.4.json` records pre-0.5 path relocations and digests; paths described as current there were current only for the historical release.
- `pre-0.5-relocations.json` checks every byte-faithful move from pre-rewrite commit `333c4f01fc93962dab7064f1181363f159644610` into this directory.

Reproducible import authority remains in `provenance/manifests/`, `provenance/evidence/`, and full Git history.
